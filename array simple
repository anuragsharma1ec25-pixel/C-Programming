#include <stdio.h>

int main()
{
    printf("enter the size of an array\n");
    int n, sum=0;
    scanf("%d", &n);
    int a[n];
    for(int i=0; i<5; i++){
        scanf("%d", &a[i]);
    }
    int max=a[0];
    int min=a[0];
    for(int i=0; i<5; i++){
        printf("%d\n", a[i]);
    }

    return 0;
}
